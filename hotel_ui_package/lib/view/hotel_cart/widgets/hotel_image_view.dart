import 'package:flutter/material.dart';

class HotelImageTest extends StatelessWidget {
  const HotelImageTest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}